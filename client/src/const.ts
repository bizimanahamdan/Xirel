export { COOKIE_NAME, ONE_YEAR_MS } from "@shared/const";

// Local auth: users sign in with an email + password on this app's own
// /login page — no external OAuth portal is involved.
export const LOGIN_PATH = "/login";

// Contact & social links — update these here and they change everywhere.
export const WHATSAPP_NUMBER = "250784198911"; // digits only, no "+", for wa.me links
export const WHATSAPP_URL = `https://wa.me/${WHATSAPP_NUMBER}`;
export const INSTAGRAM_URL = "https://www.instagram.com/xirel_outfits";
export const X_URL = "https://x.com/Xirel_store";
